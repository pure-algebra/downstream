# Pass A — domain contract for the SHA-256 specification layer (lane S1)

> **Moved document.** Authored in `mepuka/lean4-WHATWG-streams` and moved here,
> with its history, from commit `a1383bc`. Declaration names and in-package
> paths have been rewritten to their spellings here; no requirement, witness,
> or claim boundary changed. Its status line below is the status it had in
> that repository.

**Status: DRAFT for coordinator review; freezes on review.** The S1.0 seat
authored it and does not freeze it. Until it is frozen, no statement here
binds a later stage, and `docs/SHA256-DAG.md` §4 remains the only frozen
surface.

**Stage.** `docs/SHA256-DAG.md` §6 S1.0. This contract freezes the *question*
— what is transcribed, from which bytes, with which witnesses, and which
negative discriminates a right transcription from a plausible wrong one. It
freezes no code. Proof work is forbidden until S1.1 elaborates the
declarations of §4 A1.S1.

**Transcription source.** NIST FIPS 180-4, *Secure Hash Standard (SHS)*,
August 2015, vendored at `vendor/nist-fips-180-4/NIST.FIPS.180-4.pdf`,
sha256 `0455b406d89648d20cbde375561e19c245b9815e894164c2670772e3d54deb82`,
833,315 bytes, sealed by `generated/vendor-manifest.tsv` (ruling R-8). A US
Government work.

**Known-answer source.** NIST CAVP `SHA256ShortMsg.rsp`, CAVS 11.0, generated
2011-03-15, vendored at `vendor/nist-cavp-sha256/SHA256ShortMsg.rsp`,
sha256 `294ecec26959357405a621121bbfb01db4d45b9e834624b2d71aedd94ffde019`,
10,031 bytes, 65 vectors, sealed by the same manifest. The bytes were read
out of the git object of the prior-art clone
(`kim-em/lean-crypto-hash` at `54e6068abd4658fd91203cae1c2316188ffa0e89`,
blob `e14f5cbdb5d65468a0d8ada158bbcffcbf031742`), not out of its working
tree, because a Windows checkout can convert line endings and two agreeing
digests of converted bytes are not provenance (`docs/PROVENANCE.md`, "Why the
blob-hash check exists").

**Citation verification (2026-09-01, S1.0 seat).** Every FIPS 180-4 section
cited below was verified first-hand against the vendored PDF. Prose and
algorithm text were read from a liteparse markdown extraction of that file;
every table and every displayed equation was read from a page image
(`liteparse screenshot --dpi 170`), never from extracted text, because
liteparse's table reconstruction silently mangles this document — §4.1.2's
equations, for instance, extract with the `⊕` operators dropped. Physical page
numbers are the PDF's; the printed page number is given in parentheses.
Verified: **§3.1 "Bit Strings and Integers"** (physical p. 12) — verbatim,
*"the 'big-endian' convention is used when expressing both 32- and 64-bit
words, so that within each word, the most significant bit is stored in the
left-most bit position"*; **§4.1.2 "SHA-224 and SHA-256 Functions"** (physical
p. 15, printed 10) — read from the page image, equations (4.2)–(4.7) give
`Ch = (x∧y)⊕(¬x∧z)`, `Maj = (x∧y)⊕(x∧z)⊕(y∧z)`, `Σ0 = ROTR²⊕ROTR¹³⊕ROTR²²`,
`Σ1 = ROTR⁶⊕ROTR¹¹⊕ROTR²⁵`, `σ0 = ROTR⁷⊕ROTR¹⁸⊕SHR³`,
`σ1 = ROTR¹⁷⊕ROTR¹⁹⊕SHR¹⁰`; **§4.2.2 "SHA-224 and SHA-256 Constants"**
(physical p. 16, printed 11) — all sixty-four `K` words read from the page
image and found identical to the text extraction, first `428a2f98`, last
`c67178f2`, described as *"the first thirty-two bits of the fractional parts
of the cube roots of the first sixty-four prime numbers"*; **§5.1.1 "SHA-1,
SHA-224 and SHA-256"** (physical p. 18, printed 13) — read from the page
image, `ℓ + 1 + k ≡ 448 mod 512` followed by *"the 64-bit block that is equal
to the number ℓ"*, with the worked `"abc"` example giving 423 zero bits;
**§5.2.1 "SHA-1, SHA-224 and SHA-256"** (physical p. 19, printed 14) — 512-bit
blocks, sixteen 32-bit words `M₀…M₁₅`; **§5.3.2 "SHA-224"** (physical pp.
19–20, printed 14–15) — the eight initial words read from the page images,
`c1059ed8 367cd507 3070dd17 f70e5939 ffc00b31 68581511 64f98fa7 befa4fa4`;
**§5.3.3 "SHA-256"** (physical p. 20, printed 15) — read from the page image,
`6a09e667 bb67ae85 3c6ef372 a54ff53a 510e527f 9b05688c 1f83d9ab 5be0cd19`,
described as the square roots, not the cube roots, of the first eight primes;
**§6.2.2 "SHA-256 Hash Computation"** (physical p. 27, printed 22) — read from
the page image, the schedule is `Wt = Mt` for `0 ≤ t ≤ 15` and
`σ1(W t−2) + W t−7 + σ0(W t−15) + W t−16` for `16 ≤ t ≤ 63`, addition modulo
2³²; **§6.3 "SHA-224"** (physical p. 28, printed 23) — verbatim, SHA-224 is
*"defined in the exact same manner as SHA-256"* with exactly two exceptions,
the §5.3.2 initial value and truncation of the final hash value *"to its
left-most 224 bits"*.

**Ratified frame.** `docs/SHA256-DAG.md` as approved 2026-09-01: R-1 freezes
§4 A1.S1–A1.S4; R-2–R-9 stand at their defaults. This contract adds no name
and changes no statement in §4; where it names an operation it cites the
frozen name.

**Axiom allowlist.** The repository's semantic ceiling, `propext` and
`Quot.sound`, for every `Hash.Sha256.*` module (§3.1), stricter than foldlab's
`formal/fips202`, which tolerates `Classical.choice`. `Classical.choice` is
admitted only for the exact declarations ruling R-3 may name, each with its
receipt on the trust edge. `sorryAx`, `Lean.ofReduceBool`, `Lean.ofReduceNat`
and `Lean.trustCompiler` are forbidden, which forbids `native_decide` and
`bv_decide`. External recheck: `lake env leanchecker --fresh Hash.Sha256.Verified`,
and lean4lean at S1.7.

---

## 1. Domain contract

### Objects

- **Bit** = `Bool`. **Bit string** = `List Bool`, arbitrary finite length, in
  the FIPS 180-4 ordering fixed by §3.1.
- **Word** = `BitVec 32`. FIPS 180-4 §4.1.2 defines every logical function on
  32-bit words, and §3.1 fixes the big-endian reading of a word.
- **Byte string** = `List UInt8`. This is the carrier of the byte-level
  reference `Hash.Sha256.Impl` and of every known-answer test.
- **`ByteArray`** is not an object of this contract. It belongs to the native
  layer `Hash.Sha256.Fast` (§4 A1.S4) and to the public API (§4 A1.S3), and enters
  the claim path only through `Fast.sha256_eq_impl`.
- **State** = `Vector Word 8`; **block** = `Vector Word 16`; **schedule** =
  `Vector Word 64`. Sizes are carried by the type, so no length side
  condition is needed on any round lemma.

### Operations

All total, all terminating without fuel, none partial. The names are the
frozen ones of `docs/SHA256-DAG.md` §4 A1.S1; this contract introduces none.

*Bit-level transcription, `Hash.Sha256.Spec`.* `Ch`, `Maj` (§4.1.2 equations 4.2,
4.3); `Σ0`, `Σ1`, `σ0`, `σ1` (§4.1.2 equations 4.4–4.7); `K : Vector Word 64`
(§4.2.2); `H0 : Vector Word 8` (§5.3.3); `pad : List Bool → List Bool`
(§5.1.1); `parse : List Bool → List (Vector Word 16)` (§5.2.1);
`schedule : Vector Word 16 → Vector Word 64` (§6.2.2 step 1);
`compress : Vector Word 8 → Vector Word 16 → Vector Word 8` (§6.2.2 steps
2–4); `hash`; `bitsOfWords` (§3.1, big-endian, most significant bit first);
`sha256 : List Bool → List Bool`; `bitsOfBytes` / `bytesOfBits` (§3.1);
`sha256_bytes`.

*Byte-level reference, `Hash.Sha256.Impl`.* `padBytes`, `wordOfBytes`,
`wordsOfBlock`, `schedule`, `compress`, `blocks`, `hash`, `bytesOfWord`,
`sha256`. `Impl` shares `Spec`'s `Ch`, `Maj`, `Σ0`, `Σ1`, `σ0`, `σ1`, `K` and
`H0` directly rather than restating them: the two layers differ only in how
bytes become words and how blocks are cut, which is what makes the bridge a
data-flow proof rather than an arithmetic one.

`Spec.parse` and `Spec.hash` are total extensions. They carry FIPS 180-4
meaning only on padded input; on unpadded input they are defined and
meaningless. Nothing in this contract claims otherwise.

### Observables and equivalence

The only observable is the output byte string, or its bit string at the
`Spec` layer. Equality is propositional equality of outputs. There is no
observation mask, no timing observable, and no intermediate state is
observable: `compress` is compared only through the digest it contributes to,
except where a frozen lemma names it directly.

Refinement is extensional and runs in one direction at each layer:
`Impl.sha256 msg = Spec.sha256_bytes msg` on byte strings (§4 A1.S2
`Bridge.sha256_bridge`), and
`(Fast.sha256 bs).data.toList = Impl.sha256 bs.data.toList` (§4 A1.S4
`Fast.sha256_eq_impl`).

### Scope

**IN.** SHA-256 over byte-aligned messages: the §4.1.2 word functions, the
§4.2.2 constants, the §5.3.3 initial value, §5.1.1 padding, §5.2.1 parsing,
the §6.2.2 hash computation, §3.1 bit and byte ordering, the pinned CAVP
short-message vectors, and the two refinements above. SHA-224 enters at S1.6
as the discriminating negative's positive twin.

**OUT, explicitly.** Security properties of any kind — collision resistance,
preimage resistance, indifferentiability, length-extension behaviour. The
hash's injectivity. Conformance beyond the pinned vectors. Performance as a
theorem; every timing number in this lane is a measurement and is reported as
one. Bit-level, non-byte-aligned messages at the executable layer. The
`Msg`/`Len` pairs of the CAVP long-message file. SHA-384, SHA-512,
SHA-512/224, SHA-512/256, HMAC, HKDF. Streaming (S1.5, after the bridge).

### Positive examples (witnesses)

Transcribed by the S1.0 seat from the vendored `.rsp`, by
`Select-String -Path vendor/nist-cavp-sha256/SHA256ShortMsg.rsp -Pattern '^Len = <n>$' -Context 0,2`,
never from memory and never from `docs/SHA256-DAG.md`. They agree with the
coordinator's §3.4 reading in every character.

| Witness | `Len` (bits) | `Msg` | `MD` |
| --- | --- | --- | --- |
| W1 | 0 | *(empty; the file writes the placeholder `00`, see E5)* | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| W2 | 24 | `b4190e` | `dff2e73091f6c05e528896c4c831b9448653dc2ff043528f6769437bc7b975c2` |

### Forbidden example (the discriminating negative)

**SHA-224 is not SHA-256 truncated, and SHA-256 with the SHA-224 initial
value is not SHA-224 either.** FIPS 180-4 §6.3 states that SHA-224 is defined
in the exact same manner as SHA-256 with exactly two exceptions: the initial
hash value comes from §5.3.2, and the final hash value is truncated to its
left-most 224 bits. The two initial values differ in all eight words:

| | §5.3.3 SHA-256 | §5.3.2 SHA-224 |
| --- | --- | --- |
| H₀ | `6a09e667` | `c1059ed8` |
| H₁ | `bb67ae85` | `367cd507` |
| H₂ | `3c6ef372` | `3070dd17` |
| H₃ | `a54ff53a` | `f70e5939` |
| H₄ | `510e527f` | `ffc00b31` |
| H₅ | `9b05688c` | `68581511` |
| H₆ | `1f83d9ab` | `64f98fa7` |
| H₇ | `5be0cd19` | `befa4fa4` |

The initial value is load-bearing and nothing else in the algorithm reveals a
mistake in it: a transcription that used the §5.3.2 words would satisfy every
structural theorem in §4 A1.S1 — every length lemma, every padding lemma,
every round identity — and produce a wrong digest for every input. The
negative pins it. Its S1.2 form is stated on `Spec.H0 ≠ sha224IV` by `decide`;
its final form is `Bridge.sha256_ne_sha224_iv` at S1.6, when `Impl.hashWith`
exists. **Injectivity of `compress` is not claimed** in either form, and the
negative does not need it: it is a statement about two named constants and
the digests they produce on a named input, not about the round function.

A second, weaker negative is available at no cost and should be taken: the
constants of §4.2.2 are the cube roots of the first sixty-four primes and
those of §5.3.3 are the *square* roots of the first eight, so `K[0]` and
`H0[0]` are unrelated; a transcription that seeded `H0` from the `K` table's
head would be caught by W1 alone.

### Edge cases

- **E1 — the last length whose padding fits one block.** `Len = 440`, 55
  bytes. `Msg`
  `3ebfb06db8c38d5ba037f1363e118550aad94606e26835a01af05078533cc25f2f39573c04b632f62f68c294ab31f2a3e2a1a0d8c2be51`,
  `MD` `6595a2ef537a69ba8583dfbf7f5bec0ab1f93ce4c8ee1916eff44a93af5749c4`.
  With `ℓ = 440`, `ℓ + 1 + k ≡ 448 mod 512` gives `k = 7`, so the `1` bit, the
  zero run and the 64-bit length exactly fill the first block.
- **E2 — padding spills into a second block.** `Len = 448`, 56 bytes. `Msg`
  `2d52447d1244d2ebc28650e7b05654bad35b3a68eedc7f8515306b496d75f3e73385dd1b002625024b81a02f2fd6dffb6e6d561cb7d0bd7a`,
  `MD` `cfb88d6faf2de3a69d36195acec2e255e2af2b7d933997f348e09f6ce5758360`.
  `k = 511`; an implementation that computed the zero count as
  `448 − (ℓ + 1)` in `Nat` rather than modulo 512 truncates to zero here and
  loses the second block. This is the case `Impl.length_padBytes_eq` pins.
- **E3 — exactly one block of message, so padding is a whole extra block.**
  `Len = 512`, 64 bytes. `Msg`
  `5a86b737eaea8ee976a0a24da63e7ed7eefad18a101c1211e2b3650c5187c2a8a650547208251f6d4237e661c7bf4c77f335390394c37fa1a9f9be836ac28509`,
  `MD` `42e61e174fbb3897d6dd6cef3dd2802fe67b331953b06114a65c772859dfc1aa`.
  A `blocks` function that stopped when the remaining list was empty *before*
  appending the padding block, or one that emitted a spurious empty final
  block, is caught here and by nothing else in W1–E2.
- **E4 — the bit-order trap, and the one place FIPS 202 experience is
  actively misleading.** FIPS 180-4 §3.1 is **MSB-first within a word and
  within a byte**: *"within each word, the most significant bit is stored in
  the left-most bit position"*. FIPS 202 Appendix B.1 is **LSB-first within
  each byte** (`T[8i+j] = b_ij` with `h_i = Σ b_ij·2^j`). The two standards
  disagree, so foldlab's `formal/fips202` `bitsOfBytes` is **not** reusable
  here and must not be copied. A `bitsOfBytes` written to the FIPS 202
  convention passes `bytesOfBits_bitsOfBytes` and its converse — both
  round-trip lemmas hold for either convention — and fails only against a
  known-answer vector. W1 catches it; no structural theorem does.
- **E5 — the CAVP placeholder byte.** The `Len = 0` record of the vendored
  `.rsp` reads `Msg = 00`. That is one byte of hex text for a zero-length
  message, not a one-byte message: the file's own `Len` field is the
  authority and every other record's hex length is exactly `Len / 8` bytes
  (verified across all 65 records). A harness that read `Msg` and ignored
  `Len` would hash the single byte `0x00`, whose digest is
  `6e340b9cffb37a989ca544e6bb780a2c78901d3fb33738768511a30617afa01d`, not W1.
  The S1.4 runtime re-check of the vectors against the pinned file
  (`docs/SHA256-DAG.md` §8) must handle this record explicitly.

### Counterexamples to the strongest tempting overclaims

- **C1 — "the hash is injective."** False by pigeonhole: the domain is
  infinite and the codomain has 2²⁵⁶ elements. Injectivity appears nowhere in
  this lane, not even for `compress`. `pad` is injective as an encoding, and
  that is the only injectivity anything here may rely on; it is not stated as
  a §4 theorem and must not be quietly assumed by a bridge proof.
- **C2 — "the Lean `Spec` *is* FIPS 180-4."** Not a provable statement. The
  fidelity of a Lean transcription to an English-and-pseudocode standard is a
  named human-trust step. It is mitigated, not discharged, by: the pinned PDF
  digest; the section-by-section citation verification above; W1, W2, E1–E3
  from the pinned CAVP file; and the standard's own worked `"abc"` padding
  example. Every project in the surveyed prior art carries this same
  irreducible step. This contract states it rather than hiding it.
- **C3 — "the vectors passing means the implementation is correct."** 65
  short messages are finite evidence. They are not a theorem and they say
  nothing about the 56-to-64-byte boundary beyond E1–E3, nothing about large
  inputs, and nothing about `Fast`. The theorem that carries the claim is
  `Bridge.sha256_bridge`, and it carries it only for byte-aligned messages.
- **C4 — "two agreeing digest implementations means the bytes are right."**
  Refuted first-hand in this repository at P0 (`docs/PROVENANCE.md`, "Why the
  blob-hash check exists"): the in-tree SHA-256 and PowerShell `Get-FileHash`
  agreed on 8,401 bytes of EOL-converted `index.bs`, and the seal passed.
  Agreement of two hashes measures the same bytes twice. This is why the
  `.rsp` vendored at S1.0 was read from a git blob object, not a working tree.

### Assumptions, facts to prove, and deployment facts

**Assumptions (trusted, named).** Transcription fidelity of `Spec` to FIPS
180-4 (C2). The Lean v4.33.1 kernel, and the external rechecks that reduce
but do not remove reliance on it (`leanchecker`, lean4lean at S1.7). The
integrity of the two vendored files, resting on their sealed digests and, for
the `.rsp`, on the upstream git blob hash. The host and its hardware.

**Facts to prove.** Exactly the theorem list of §4 A1.S1–A1.S4 and no more:
the `Spec` length, padding and round-trip lemmas; the `Impl` length lemmas;
the seven bridge theorems with `Bridge.sha256_bridge` as their apex; the
`Fast` abstraction lemmas with `Fast.sha256_eq_impl` as theirs; the API
theorems `sha256_impl`, `sha256_spec`, `sha256_ofList`, and the `Digest`/`Hex`
facts. The obligation ledger below tracks them.

**Deployment facts (measured or tested, never claimed as theorems).** Wall
time and memory of every layer. Agreement with PowerShell `Get-FileHash` and
with the P0 tooling on real files. Byte-identical regeneration of
`generated/vendor-manifest.tsv` through the new implementation. Dual-host
agreement of axiom reports, compared as `(declaration, axiom-set)` pairs and
never as raw log lines.

### Claim-domain table

| Claim | Domain on which it is stated | Domain on which it is **not** stated |
| --- | --- | --- |
| `Bridge.sha256_bridge` | byte-aligned messages, `msg : List UInt8` | bit strings whose length is not a multiple of 8 |
| `Spec.bitsOfBytes_bytesOfBits` | bit strings with `bits.length % 8 = 0`, premise carried | arbitrary bit strings; the converse direction has no premise and needs none |
| `Spec.parse`, `Spec.hash` | padded input | unpadded input, where both are total and meaningless |
| W1, W2, E1–E3 | five byte-aligned messages of 0, 3, 55, 56 and 64 bytes | every other input |
| `Fast.sha256_eq_impl` | every `ByteArray` | nothing; this one is unrestricted |
| `lake exe sha256 --self-test` | finite executable evidence | any theorem; it carries none (`Gates/AGENTS.md`) |
| every timing number in this lane | one host, one toolchain, stated runs | any other host, and any theorem |

No unrestricted bijection is claimed anywhere. The domain-of-validity rule is
inherited from foldlab `formal/fips202` REV2.

---

## 2. Prior-art ledger

Reproduced from `docs/SHA256-DAG.md` §3.5, which is the authority; this table
is a reading aid for the seat implementing against this contract.

| Source | Guarantee offered | Mismatch with this lane | Class |
| --- | --- | --- | --- |
| foldlab `formal/fips202` (SHA3-512, fully proved `Impl = Spec`, dual-host, leanchecker) | the Pass A / Pass B contract shape; the B2a–B2f decomposition; kernel-KAT cost data; the `Spec`/`Impl`/`Fast` split and the built-in rules of §3.2–§3.3 | 64-bit lanes and a sponge, not 32-bit words and a Merkle–Damgård compression; tolerates `Classical.choice`, which this lane does not; FIPS 202 B.1 is LSB-first within a byte and FIPS 180-4 §3.1 is MSB-first, so `bitsOfBytes` is **not** reusable (E4) | process precedent |
| kim-em/lean-crypto-hash `Crypto/SHA2/*`, commit `54e6068a`, Apache-2.0, toolchain v4.33.0 | SHA-256/224/384/512 over `UInt32`/`UInt64` with `Vector` and proof-indexed loops; `ByteVector n` carrying a size proof; a `Context.update_append` streaming law; strict hex; the CAVP vectors; conformance against OpenSSL and coreutils | its own README states the theorems "are not a formal end-to-end proof of each compression function"; `validation/CryptoValidation/Proofs.lean` imports `Std.Tactic.BVDecide`, which closes through `Lean.ofReduceBool` and is forbidden here; `Id.run do` bodies are outside §3.3; it adopts Lean's module system; toolchain v4.33.0 not v4.33.1 | reference for API shape, for the streaming technique (credited at S1.5), and for the vector files; **no code imported** |
| NIST FIPS 180-4 PDF | the semantics, by definition | the prose-to-Lean gap is C2 | primary source |
| fips202 S0.2 Route A, operator-supplied 2026-09-01 | a `Classical.choice`-free byte round trip built on `lowBits`, `lowBits_spec`, `lowBits_eq_mod` and `byte_roundtrip_fin` | FIPS 202 B.1 is LSB-first and SHA-256's byte-to-bit map is MSB-first (E4), so the fold index is `7 − j` and the `testBit` argument follows | the technique for `Spec.bytesOfBits_bitsOfBytes`; reproduced, not imported |
| Lean v4.33.1 core `BitVec`/`Vector`/`List`/`Nat` API | a complete lemma base for the bit operations, `Nat.fold` induction, and `Vector` extensionality | none; zero Lake dependencies is a property of this artifact's trust statement | reuse |

---

## 3. Obligation ledger

| Obligation | Kind | Falsified by | Trust boundary |
| --- | --- | --- | --- |
| `Spec` length, padding and round-trip lemmas (§4 A1.S1) | prove | a counterexample in Lean | kernel + external recheck |
| `Impl` length lemmas, including `length_padBytes_eq` | prove | a counterexample in Lean | same |
| W1, W2, E1–E3 as compiled `#guard` on `Impl` | executable check | **any mismatch is a transcription bug and reopens this contract; the literal is never adjusted** | the pinned `.rsp`, sealed |
| W1 as `decide +kernel` on `Impl` | prove, kernel reduction | any mismatch | kernel alone, no compiler |
| the seven `Bridge` theorems, apex `sha256_bridge` | prove | a counterexample in Lean | kernel + external recheck |
| the SHA-224 initial-value negative | prove on named constants | — | same |
| the `Fast` abstraction lemmas, apex `sha256_eq_impl` | prove | a counterexample in Lean | same |
| transcription fidelity | **assumption** (C2) | any CAVP divergence; any citation that fails to verify against the pinned PDF | human review against the pinned PDF |
| axiom profile within the ceiling | gate | any axiom outside `propext`/`Quot.sound` reached by any `Hash.Sha256.*` declaration | `#sha256_axiom_audit`, pinned by `#guard_msgs`; `HashTest/Audit/AxiomGate.lean`; `leanchecker --fresh` |
| byte-identical manifest regeneration through the new implementation | gate | any byte difference — a finding about one of the two implementations, never a reason to rewrite the manifest | `lake exe vendorseal` |
| throughput within 2× of the §2 baseline | **measurement** | a slower number, which is recorded, not hidden | none; it is not a theorem |

---

## 4. Counterexample rows to register

Each becomes a row in `test/counterexamples/REGISTER.md` with a stable
`HASH-SHA256-CE-nnn` id when its witness lands.

| Mutant | Caught by | Caught by nothing else in the set |
| --- | --- | --- |
| `H0` seeded from §5.3.2 (the SHA-224 initial value) | W1 | yes — every structural theorem still holds |
| padding that omits the 64-bit length field | W1, W2 | no |
| zero count computed as `448 − (ℓ+1)` in `Nat` instead of modulo 512 | E2 | yes |
| little-endian word loading in `wordOfBytes` | W2 | no; W1 has no message words |
| `bitsOfBytes` written LSB-first, the FIPS 202 convention | W1 (E4) | yes — both round-trip lemmas still hold |
| `blocks` dropping the final padding-only block | E3 | yes |
| `Msg = 00` of the `Len = 0` record read as a one-byte message | W1 (E5) | yes |

---

## 5. Handoffs

1. → **coordinator**: review and freeze this contract, or return it. Two
   items need a decision beyond review: whether E5 and C4 belong in the
   contract or in `docs/PROVENANCE.md`, and whether the second, weaker
   negative in the "Forbidden example" section is worth a registered row.
2. → **the one-shot S1.1–S1.4 seat** (ruling R-10, which removes the separate
   breaker for this lane): implement §4 A1.S1 exactly, with each `Spec`
   definition carrying its FIPS 180-4 section in a docstring, and each
   section verified against the vendored PDF the way the
   citation-verification paragraph above was produced. **Re-transcribe W1,
   W2 and E1–E3 from the vendored `.rsp` rather than copying them out of this
   document**: this contract is a reading aid for the vectors, never their
   authority. Under R-10 the coordinator's first-hand review replaces the
   breaker's independent freeze, so the seat carries the burden of not
   letting a literal reach the battery by any route except the pinned file.
   Any statement that cannot be closed is left out of the green build and
   parked in a module declared in `test/fixtures/trust-gate/known-red.txt`,
   never closed with `sorry`.
