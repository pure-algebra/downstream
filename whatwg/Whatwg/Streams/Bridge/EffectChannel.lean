/-!
# Bridge.EffectChannel.lean

Owner: the embedding between this calculus and the Effect Channel calculus
of the companion repository, with the loss and the mask each direction
names.

Spec anchors: none; the companion repository is a cross-repository pin.

Opens in P12.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
