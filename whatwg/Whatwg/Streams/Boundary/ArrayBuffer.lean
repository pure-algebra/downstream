/-!
# Boundary.ArrayBuffer.lean

Owner: the ArrayBuffer profile: transfer and detachment as foreign answers,
and the view records the byte algorithms read, none of which is modelled as
a host object.

Spec anchors: `rbs-controller-abstract-ops`, `misc-abstract-ops`.

Opens in P9.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
