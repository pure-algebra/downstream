/-!
# Boundary.Transformer.lean

Owner: the transformer profile: the start, transform, flush and cancel calls
as decisions on the tape, their answer kinds, and the settlement timing each
answer carries.

Spec anchors: `transformer-api`.

Opens in P6.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
