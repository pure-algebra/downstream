/-!
# Boundary.UnderlyingSource.lean

Owner: the underlying-source profile: the start, pull and cancel calls as
decisions on the tape, their answer kinds, and the settlement timing each
answer carries.

Spec anchors: `underlying-source-api`.

Opens in P4.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
