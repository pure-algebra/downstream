/-!
# Data.Chunk.lean

Owner: the chunk universe: the first-order values a stream carries, the
ordinary and byte-view families kept apart, and the equality on chunks that
an observation mask compares.

Spec anchors: `model`, `rs-model`, `conventions`.

Opens in P3.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
