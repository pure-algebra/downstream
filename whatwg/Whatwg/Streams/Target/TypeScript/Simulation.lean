/-!
# Target.TypeScript.Simulation.lean

Owner: the simulation between a lowered program and its source combinator
under a named mask, and the host observations that are evidence for it and
never a proof of it.

Spec anchors: none; TypeScript is one target profile, never the semantic
owner.

Opens in P11.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
