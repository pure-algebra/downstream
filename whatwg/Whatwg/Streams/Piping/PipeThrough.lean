/-!
# Piping.PipeThrough.lean

Owner: `pipeThrough` over a readable and writable pair: the locking check it
performs and the pipe it starts, whose meaning is the pipe-to relation.

Spec anchors: `rs-prototype`, `rs-abstract-ops`.

Opens in P7.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
