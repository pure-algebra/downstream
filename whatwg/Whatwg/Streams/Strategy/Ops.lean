/-!
# Strategy.Ops.lean

Owner: the queuing-strategy abstract operations: extracting a high-water
mark and a size algorithm from a strategy record, and validating and
normalizing a supplied mark.

Spec anchors: `qs-api`, `qs-abstract-ops`.

Opens in P3.

The data these operations produce is owned by
`Whatwg/Streams/Data/Strategy.lean`.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
