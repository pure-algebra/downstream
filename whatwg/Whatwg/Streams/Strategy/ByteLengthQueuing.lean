/-!
# Strategy.ByteLengthQueuing.lean

Owner: the `ByteLengthQueuingStrategy` class: its construction from an init
record, its high-water mark, and its size function, which answers the byte
length of a chunk.

Spec anchors: `blqs-class`.

Opens in P3.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
