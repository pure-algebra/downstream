/-!
# Strategy.CountQueuing.lean

Owner: the `CountQueuingStrategy` class: its construction from an init
record, its high-water mark, and its size function, which answers one for
every chunk.

Spec anchors: `cqs-class`.

Opens in P3.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
