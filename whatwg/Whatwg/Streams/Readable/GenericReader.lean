/-!
# Readable.GenericReader.lean

Owner: the reader mixin shared by both reader kinds: the closed promise,
cancel, and the acquire and release discipline that locks a stream to one
reader.

Spec anchors: `generic-reader-mixin`, `generic-reader-internal-slots`.

Opens in P4.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
