/-!
# Readable.AsyncIteration.lean

Owner: the async-iteration protocol over a readable stream: the iterator
record, its next and return steps, and the prevent-cancel option that
governs release.

Spec anchors: `rs-asynciterator`.

Opens in P4, after the default-path representative closes.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
