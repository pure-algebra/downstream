/-!
# Readable.Byte.ByobRequest.lean

Owner: the `ReadableStreamBYOBRequest` view handed to an underlying byte
source, and the respond and respond-with-new-view operations that close it.

Spec anchors: `rs-byob-request-class`, `rs-byob-request-internal-slots`.

Opens in P9.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
