/-!
# Readable.Byte.Controller.lean

Owner: the `ReadableByteStreamController` state and its abstract operations:
the byte queue, the auto-allocation size, and enqueue, respond and close
over byte views.

Spec anchors: `rbs-controller-class`, `rbs-controller-internal-slots`,
`rbs-controller-abstract-ops`.

Opens in P9.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
