/-!
# Readable.Byte.PullInto.lean

Owner: the pull-into descriptor queue: buffer, byte offset, byte length,
bytes filled, minimum fill, element size, view constructor, and the reader
kind each descriptor serves.

Spec anchors: `rbs-controller-internal-slots`,
`rbs-controller-abstract-ops`.

Opens in P9.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
