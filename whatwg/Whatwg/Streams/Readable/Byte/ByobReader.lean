/-!
# Readable.Byte.ByobReader.lean

Owner: the `ReadableStreamBYOBReader` state and its read-into requests: the
caller-supplied view, the minimum fill, and the answers that satisfy them.

Spec anchors: `byob-reader-class`, `byob-reader-internal-slots`.

Opens in P9.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
