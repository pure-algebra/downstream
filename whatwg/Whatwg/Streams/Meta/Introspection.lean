/-!
# Meta.Introspection.lean

Owner: first-order introspection over the library declarations: names,
signatures and digests as data, never a stored elaborator expression.

Spec anchors: none; this is repository machinery, not specification content.

Opens in P11.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet.
-/
