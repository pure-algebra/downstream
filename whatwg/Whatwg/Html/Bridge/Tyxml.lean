/-!
# Html.Bridge.Tyxml.lean

Owner: the port-fidelity statement: a TyXML term over `Html_sigs.T` typechecks in OCaml exactly when its image is admitted here, stated over the schema projection and witnessed by TyXML's own test corpus at the pin.

Schema anchors: `test/test_html.ml` and `test/html_fail.ml` of the pinned TyXML tree (not yet vendored; see the plan).

Opens in H6.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet
(`docs/HTML-PACKAGE-PLAN.md`).
-/
