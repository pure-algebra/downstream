/-!
# Html.Content.Divergence.lean

Owner: the authored list of places where TyXML's typing at the pin departs from the HTML Standard's content models at its pin (obsolete elements, missing elements, transparent approximated as flow-without-interactive, XHTML-style void serialization), each with its span digest in `source`.

Schema anchors: `vendor/whatwg-html-746f2ede/source`, per element section; ruling HP-2.

Opens in H3, extended through H6.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet
(`docs/HTML-PACKAGE-PLAN.md`).
-/
