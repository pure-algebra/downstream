/-!
# Html.Audit.Receipts.lean

Owner: per-packet axiom receipts and closure snapshots for this library, in the shape `Whatwg/Streams/Audit` uses.

Schema anchors: none.

Opens in H3 onward.

This breadth stub intentionally declares no semantic object. Its public
surface is frozen only after the owning contract and counterexample packet
(`docs/HTML-PACKAGE-PLAN.md`).
-/
