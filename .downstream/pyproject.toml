[project]
name = "downstream"
version = "0.0.0"
description = "Scripts for managing lake package monorepos"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
preview = true

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
